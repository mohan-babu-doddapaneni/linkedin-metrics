{
  "name": "linkedin-exact-metrics",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome extension that shows exact applicant/view counts and posting age on LinkedIn job listings.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
